# Public browser key: enable Maps JavaScript API and restrict it to your local
# and deployed website origins (for example, http://localhost:3000/*).
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=

# Server-only key: enable Places API (New) and restrict it to the production
# server / Worker. Do not use this key in client-side code.
GOOGLE_PLACES_API_KEY=
